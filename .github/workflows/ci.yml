name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  check:
    name: lint + typecheck + test
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        python-version: ["3.11", "3.12"]

    steps:
      - uses: actions/checkout@v4

      - name: Set up Python ${{ matrix.python-version }}
        uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python-version }}
          cache: pip
          cache-dependency-path: pyproject.toml

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          # Install the web extra alongside dev so:
          #  (1) mypy can resolve fastapi/jinja2/bleach/markdown imports
          #      reachable from src/harness_weaver/web (otherwise the
          #      module's top-level imports trip "Cannot find stubs for
          #      module"),
          #  (2) the web test suite actually runs in CI rather than
          #      skipping via pytest.importorskip.
          pip install -e ".[dev,web]"

      - name: Ruff format check
        run: ruff format --check src tests

      - name: Ruff lint
        run: ruff check src tests

      - name: Mypy
        run: mypy src

      - name: Pytest
        run: pytest
        env:
          # Tests must not require live API keys; cassettes only.
          ANTHROPIC_API_KEY: dummy-key-for-cassette-replay

      - name: Upload coverage
        if: matrix.python-version == '3.11'
        uses: actions/upload-artifact@v4
        with:
          name: coverage-${{ matrix.python-version }}
          path: |
            .coverage
            coverage.xml
          if-no-files-found: ignore
